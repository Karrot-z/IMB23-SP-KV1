#include <iostream>
using namespace std;

int main(){
    int a,b,c,d,e;
    cin>>a>>b;
    if (a<=0 or b<=0) {
        cout<<"Invalid input"<<endl;
        return 0;
    }
    if (b>a) {
        c=a;
        a=b;
        b=c;
    }

    bool parnapos = false;
    c=a;

    while (b>0) {
        d=b%10;
        b/=10;
        bool found = false;

        while (found == false and c>0) {
            e=c%10;
            c/=10;
            if (parnapos == true && e==d) {
                found = true;
            }else if (parnapos == true) {
                cout<<"NE";
                return 0;
            }

            if (parnapos == false)
                parnapos = true;
            else
                parnapos = false;
        }
    }

    cout<<"PAREN";
    return 0;
}
