#include <iostream>
using namespace std;

int main(){
    int n,m,d,c,rez=1;
    cin>>n>>m;

    for(int i=n; i<=m; i++){
        d=i;
        if (d==0) {
            rez=i;
            break;
        }else if (d<0)
            d*=-1;
        while (d>0) {
            c=d%10;
            d/=10;
            if (c%2==0) {
                if (d==0)
                    rez=i;
            }else
                continue;

        }
        if (rez!=1)
            break;
    }

    if (rez!=1)
        cout<<rez;
    else
        cout<<"NE";

    return 0;
}
